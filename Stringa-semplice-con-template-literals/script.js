const nome = 'Vincent';
const cognome = 'Santana';

// Creare una stringa che li combini utilizzando i template literals
const persona = `${nome} ${cognome}`;

// Stampare la stringa risultante nella console
console.log(persona);