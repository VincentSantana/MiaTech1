// Dichiarare una funzione che contenga il messaggio
function stampaMessaggio() {
  console.log('Il messaggio è stato stampato dopo 3 secondi!');
}

// Utilizzare setTimeout per eseguire la funzione dopo 3 secondi
setTimeout(stampaMessaggio, 3000);